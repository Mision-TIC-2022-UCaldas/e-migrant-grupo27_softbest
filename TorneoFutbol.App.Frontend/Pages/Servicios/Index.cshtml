@page
@using TorneoFutbol.App.Dominio
@model TorneoFutbol.App.Frontend.Pages.Servicios.IndexModel
@{
}

<form method="get">
<div class="form-inline">
    <label class="sr-only" for="g">Filtro</label>
    <input type="text" name="g" id="g" asp-for="@Model.gActual"  class="form-control mb-2 mr-sm-2"
         placeholder="Filtrar por Nombre" />
         
        <button type="submit" class="btn btn-primary mr-sm-2 mb-2">Filtrar</button>          

</div>
    
</form>
<h1>Listado de Servicios</h1>


<table class="table">
    <thead class="thead-dark">
        <tr>
            <th scope="col">Nombre</th>
            <th scope="col">Numero_Migrantes</th>
            <th scope="col">Estado_Servicio</th>
            <th scope="col">Acciones</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var servicio in Model.servicio)
        {
            <tr>
                <td>@servicio.Nombre</td>
                <td>@servicio.Numero_Migrantes</td>
                <td>@servicio.Estado_Servicio</td>
            <td>
                <a class="btn btn-secondary" asp-page="./Details" asp-route-id="@servicio.Id">Detalle</a>
                <a class="btn btn-primary" asp-page="./Edit" asp-route-id="@servicio.Id">Editar</a>
            </td>
            </tr>
        }
        
    </tbody>
    
</table>
<a class="btn btn-primary" asp-page="./Create">Crear Servicio</a>
